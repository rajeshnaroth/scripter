import { HandleMIDI, ParameterChanged, ProcessMIDI, Idle } from "./ragaTranslator";

// Remove these lines once you copy the built files from dist
HandleMIDI();
ParameterChanged();
ProcessMIDI();
Idle();
